import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(theme => ({
  img: {
    width: '100%'
  },
  textField: {
    width: '100%'
  },
  typography: {
    textAlign: 'center'
  },
  submit: {
    marginTop: 20
  }
}));
